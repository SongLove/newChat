
import headTab from '../components/headTab'
import Scroll from '../components/scroll'
import Loading from '../components/loading'

export default (Vue) => {
  Vue.component('HeadTab', headTab)
  Vue.component('Scroll', Scroll)
  Vue.component('Loading', Loading)
}